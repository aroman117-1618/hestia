# Hestia - Personal AI Assistant Dependencies
#
# This is the HUMAN-EDITED input file. Do NOT edit requirements.txt directly.
# Target: Python >=3.11,<3.13 (matches CI and pyproject.toml)
#
# Workflow:
#   1. Edit this file (add/remove/update packages)
#   2. Recompile targeting Python 3.11 (works from any local Python):
#      uv pip compile requirements.in --python-version 3.11 --output-file requirements.txt --no-emit-index-url
#   3. Commit BOTH files

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Vector Database for Memory
chromadb>=0.4.0

# Configuration
pyyaml>=6.0

# Security
cryptography>=41.0.0
keyring>=24.0.0
python-jose[cryptography]>=3.3.0

# HTTP Client
httpx>=0.25.0

# Token Counting
tiktoken>=0.5.0

# Additional utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
aiosqlite>=0.19.0

# Scheduling
apscheduler>=3.10.0

# QR Code generation
qrcode[pil]>=7.4.0

# Content Extraction (Investigate module)
trafilatura>=1.6.0
youtube-transcript-api>=0.6.0

# Fuzzy String Matching (Apple metadata cache)
rapidfuzz>=3.0.0

# Trading Module (core — required for BotRunner)
pandas>=2.0.0
ta>=0.10.0
coinbase-advanced-py>=1.0.0
alpaca-py>=0.30.0,<1.0
aiohttp>=3.9.0

# Trading Module (backtesting only — requires Python 3.10+)
# vectorbt>=0.26.0  # Deferred: Mac Mini runs 3.9, not needed for paper soak

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.2.0
