---
name: restart
description: "[DEPRECATED] Use /preflight instead — it includes all restart functionality plus environment validation"
user_invocable: false
allowed_tools:
  - Bash
  - Read
---

# DEPRECATED — Use /preflight

This skill has been merged into `/preflight`. Run `/preflight` instead — it kills stale processes, restarts the server, runs tests, and performs additional environment checks.
